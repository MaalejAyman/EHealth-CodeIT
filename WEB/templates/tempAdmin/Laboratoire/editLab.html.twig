{% extends 'tempAdmin/adminbase.html.twig' %}
{% block content %}
    <section id="container">
        <section id="main-content">
            <section class="wrapper">
                <div class="row" style="margin-left: 2%;margin-right: 2%">
                    <table class="table table-striped form-horizontal" style="margin-top: 1%">
                        <h1>les Details du Laboratoire</h1>

                        {{ include('tempAdmin/Laboratoire/_formEdit.html.twig', {'button_label': 'Modifier ce Laboratoire'}) }}

                        <a href="{{ path('laboratoire_index') }}"><button class="btn btn-theme start" >Retourner a la liste</button></a>

                        {{ include('tempAdmin/Laboratoire/_delete_form.html.twig') }}
                    </table>
                </div>
            </section>
        </section>
    </section>
{% endblock %}