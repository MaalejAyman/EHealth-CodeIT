{% extends 'tempClient/baseP.html.twig' %}
    {% block top_menu %}
        <section class="navbar custom-navbar navbar-fixed-top" role="navigation">
            <div class="container">
                <div class="navbar-header">
                    <button class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                        <span class="icon icon-bar"></span>
                        <span class="icon icon-bar"></span>
                        <span class="icon icon-bar"></span>
                    </button>
                    <!-- lOGO TEXT HERE -->
                    <a href="#" class="navbar-brand">E-Health</a>
                </div>
                <div class="collapse navbar-collapse">
                    <ul class="nav navbar-nav navbar-nav-first">
                        <li>
                            <a href="{{ path('pageAcc' )}}" id="index">
                                Acceuil
                            </a>
                        </li>
                        <li>
                            <a href="{{ path('profile_edit', {'id': app.user.id}) }}" id="edit">
                               Profile
                            </a>
                        </li>
                        <li>
                            <a href="{{ path('ConsulterRendezVous') }}" id="rendezvous">
                                Rendez-vous
                            </a>
                        </li>
                        <li>
                            <a href="{{ path('ListerQuestion') }}" id="Questions">
                                Questions
                            </a>
                        </li>

                    </ul>
                    <ul class="nav pull-right top-menu" style="margin: 1%">
                        {% if app.user %}
                        <li><a class="btn btn-theme04 delete" href="{{ path('app_logout') }}">Deconnexion</a></li>
                       {% endif %}

                    </ul>
                </div>
            </div>
        </section>

    {% endblock %}

{% block content %}

{% endblock %}




